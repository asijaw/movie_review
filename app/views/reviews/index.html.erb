<% @reviews.each do |review| %>
    <p>
        <strong><%= review.movie.title.titleize %></strong></br>
        <%= review.content %></br>
        <%= review.rating %> stars</br>
        by: <%= link_to review.user.username, user_reviews_path(review.user.id) %>
    </p>

<% end %>